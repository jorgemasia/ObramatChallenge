<div>
  <button routerLink="/orders/new" mat-raised-button color="primary">Create Order</button>
</div>

<div *ngIf="orders$ | async as orders">
    <table mat-table [dataSource]="orders" class="mat-elevation-z8">

    <ng-container matColumnDef="id">
        <th mat-header-cell *matHeaderCellDef>ID</th>
        <td mat-cell *matCellDef="let order">{{ order.id }}</td>
    </ng-container>

    <ng-container matColumnDef="createdAt">
        <th mat-header-cell *matHeaderCellDef>Created At</th>
        <td mat-cell *matCellDef="let order">{{ order.createdAt | date }}</td>
    </ng-container>

    <ng-container matColumnDef="productCount">
        <th mat-header-cell *matHeaderCellDef>Products</th>
        <td mat-cell *matCellDef="let order">{{ order.productCount }}</td>
    </ng-container>

    <ng-container matColumnDef="totalWithoutTax">
        <th mat-header-cell *matHeaderCellDef>Total w/o Tax</th>
        <td mat-cell *matCellDef="let order">{{ order.totalWithoutTax | currency }}</td>
    </ng-container>

    <ng-container matColumnDef="totalWithTax">
        <th mat-header-cell *matHeaderCellDef>Total w/ Tax</th>
        <td mat-cell *matCellDef="let order">{{ order.totalWithTax | currency }}</td>
    </ng-container>

    <ng-container matColumnDef="status">
        <th mat-header-cell *matHeaderCellDef>Status</th>
        <td mat-cell *matCellDef="let order">
        <span [ngClass]="{
            'pending': order.status==='Pending',
            'cancelled': order.status==='Cancelled',
            'paid': order.status==='Paid'
        }">{{ order.status }}</span>
        </td>
    </ng-container>

    <ng-container matColumnDef="actions">
        <th mat-header-cell *matHeaderCellDef>Actions</th>
        <td mat-cell *matCellDef="let order">
        <button mat-button color="primary" [routerLink]="['/', 'orders', order.id]">View</button>
        <button mat-button color="accent" [routerLink]="['/', 'orders', order.id, 'edit']">Edit</button>
        <button mat-button color="warn" (click)="deleteOrder(order.id)">Delete</button>
        </td>
    </ng-container>

    <tr mat-header-row *matHeaderRowDef="displayedColumns"></tr>
    <tr mat-row *matRowDef="let row; columns: displayedColumns;"></tr>
    </table>
</div>